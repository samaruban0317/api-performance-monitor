# Development & docs-only dependencies.
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
# (runtime deps live in requirements.txt)

# Used by scripts/screenshots.py to capture README dashboard images.
# After installing, fetch the browser binary:  python -m playwright install chromium
playwright==1.60.0
